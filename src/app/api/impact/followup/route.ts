import { NextResponse } from "next/server";
import { authorizeCapability } from "@/lib/auth/authorize";
import { followUpCreateSchema } from "@/lib/domain/phase1-contracts";
export async function GET(){const auth=await authorizeCapability("impact.read");if(!auth.ok)return NextResponse.json({error:auth.error},{status:auth.status});const{data,error}=await auth.supabase.from("follow_up_records").select("id,program_id,followup_type,scheduled_date,status,completed_date,notes,created_at,programs(title)").is("voided_at",null).order("scheduled_date",{ascending:true});return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({data});}
export async function POST(request:Request){const auth=await authorizeCapability("impact.manage");if(!auth.ok)return NextResponse.json({error:auth.error},{status:auth.status});const parsed=followUpCreateSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:parsed.error.issues.map(i=>i.message).join("; ")},{status:400});const{data,error}=await auth.supabase.rpc("phase1_create_impact_record",{p_entity_type:"follow_up",p_payload:parsed.data});return error?NextResponse.json({error:error.message},{status:error.code==="42501"?403:422}):NextResponse.json({data:{id:data}},{status:201});}
