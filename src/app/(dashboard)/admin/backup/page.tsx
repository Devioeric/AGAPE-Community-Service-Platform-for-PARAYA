import { DatabaseBackup, ShieldAlert } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
export default function BackupRunbookPage(){return <div className="space-y-5"><div><h1 className="font-heading text-2xl font-bold">Backup & recovery</h1><p className="text-sm text-muted-foreground mt-1">Infrastructure runbook only; resident and operational data are not browsable from the Admin portal.</p></div><Card><CardContent className="p-5 flex gap-3"><DatabaseBackup className="text-primary shrink-0"/><div><h2 className="font-semibold">Use Supabase-managed backups</h2><p className="text-sm text-muted-foreground mt-1">Plaintext application exports and browser-uploaded restore commits are disabled. Recovery must use an approved, encrypted, purpose-bound process with a tested rollback and immutable audit record.</p></div></CardContent></Card><Card className="border-warning/30 bg-warning/5"><CardContent className="p-5 flex gap-3"><ShieldAlert className="text-warning shrink-0"/><p className="text-sm">A future break-glass recovery flow requires recent re-authentication, a documented reason, authorization, encryption, and fail-closed auditing. Phase 1 does not grant ordinary resident access to System Admin.</p></CardContent></Card></div>}
